import styled from "styled-components";
import {theme} from "./theme"

export const Page = styled.section`
    display: block;
    height: 95vmin;
    max-width: 900px;
    width: 100%;
    margin: 0 auto;
`;