// my-theme.ts
import { Theme } from 'styled-components'

/**
 * Define theme properties
 */
export const theme: Theme = {
    colors: {
        primary: 'cyan',
        secondary: 'magenta',
    },
};